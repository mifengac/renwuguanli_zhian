Place Oracle Instant Client files in this directory before building the Docker image.

Expected container path:

- `/opt/oracle/instantclient`

Required examples:

- `libclntsh.so`
- `libnnz*.so`
- `libocci.so`
